# Modelo de variáveis de ambiente. NUNCA coloque valores reais aqui.
# O aluno copia para .env (que é gitignored) e preenche com as chaves dele.
# Leia no código com os.environ.get("NOME") — nunca escreva a chave direto.

EXEMPLO_API_KEY=
EXEMPLO_TOKEN=
